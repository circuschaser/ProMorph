<div id="header">
  <div class="container">
    
    <div id="logo">
      <% if signed_in? %>
      <%= link_to image_tag("logo.png"), "/home" %>
      <% else %>
      <%= link_to image_tag("logo.png"), "/" %>
      <% end %>
    </div>

    <% if signed_in? %>

      <div id="topbar">
  <!-- missing -->
          <div id="navmenu">
            <span><%= link_to "My Music", '#' %></span>
            <span><%= link_to "My Circles", "#" %></span>
            <span><%= link_to "My Setlists", "#" %></span>
            <span><%= link_to "My Account", current_user %></span>
  <!-- missing -->
            <div class="megamenu">
              <span><%= link_to "Admin", "#" %></span>
              <div>
                <span>You're an admin.</span><br/>
                <span>Don't let it get to your head.</span><br/>
              </div>
            </div>
  <!-- missing END -->
          </div>
          <div id="toprightbar">
            <div id="current_user_info">
              <%= link_to "Sign out", signout_path, method: "delete" %>
  <!-- missing -->
            </div>
            <div id="search_form">
              <%= form_for "/pieces" do |f| %>
              <span>Search</span> <%= f.text_field :search %>
              <% end %>
            </div>
          </div>
      </div>

    <% else %>
      <div id="topbar">
          <div id="toprightbar2">
            <%= link_to "SIGN IN HELP", "https://en.wikipedia.org/wiki/Login", :class => "btn-mini" %>
          </div>
      </div>
    <% end %>

  <!-- missing -->

      </div>
  </div>        
</div>
